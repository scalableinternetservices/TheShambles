<%= form_with(model: background, local: true) do |form| %>
  <% if background.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(background.errors.count, "error") %> prohibited this background from being saved:</h2>

      <ul>
      <% background.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name, id: :background_name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :background_price %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_area :image, id: :background_url %>
  </div>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.number_field :game_id, id: :background_appid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
