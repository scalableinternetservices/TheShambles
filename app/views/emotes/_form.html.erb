<%= form_with(model: emote, local: true) do |form| %>
  <% if emote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(emote.errors.count, "error") %> prohibited this emote from being saved:</h2>

      <ul>
      <% emote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name, id: :emote_name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :emote_price %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_area :image, id: :emote_url %>
  </div>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.number_field :game_id, id: :emote_appid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
